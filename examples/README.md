# Examples

| Path | What |
| --- | --- |
| [`patterna/`](patterna/) | Pattern A multi-agent **demo** (ZCG-33): `jobsma` + one job, Zeus Direct + AgentTurn |

Everyday Q&A stays Mode 1 (`Client.Agent().RunTurn`). These examples never auto-promote chat into `Jobs().Run`.

Claim stays **candidate**. `multi_agent=demo` is not `supported`.
